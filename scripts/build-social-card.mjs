// Draws the 1280x640 social preview. A PNG, not the SVG, because link unfurlers
// (Slack, WhatsApp, X) do not reliably rasterise SVG og:images.
// Requires Python with Pillow. Run: npm run build-social
